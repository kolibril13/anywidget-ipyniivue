{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.jsx --minify --format=esm --bundle --outdir=src/anywidget_ipyniivue/static"
	},
	"dependencies": {
		"@anywidget/react": "^0.0.2",
		"@niivue/niivue": "^0.38.8",
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"esbuild": "^0.19.5"
	}
}
